[workspace]
resolver = "2"
members = [
    "crates/bezel",
    "crates/theme",
    "crates/motion",
    "crates/agent",
    "crates/ui",
    "crates/icons",
    "crates/syntax",
    "crates/syntax-std",
    "crates/markdown",
    "crates/blocks",
    "crates/editor",
    "crates/terminal",
    "crates/canvas",
    "apps/gallery",
    "apps/web",
    "apps/hello",
    "apps/parity",
]

[workspace.package]
version = "0.1.29"
edition = "2024"
license = "MIT"
authors = ["clearloop <tianyi.gc@gmail.com>"]
repository = "https://github.com/crabtalk/bezel"
homepage = "https://github.com/crabtalk/bezel"
readme = "README.md"
keywords = ["gpui", "gui", "ui", "components"]
categories = ["gui"]
rust-version = "1.90"

[workspace.dependencies]
# The short names are taken on crates.io, so packages publish as `bezel-*`; the
# key here and each crate's `[lib] name` are what the source imports.
bezel = { path = "crates/bezel", version = "0.1.29" }
agent = { package = "bezel-agent", path = "crates/agent", version = "0.1.29" }
theme = { package = "bezel-theme", path = "crates/theme", version = "0.1.29" }
motion = { package = "bezel-motion", path = "crates/motion", version = "0.1.29" }
ui = { package = "bezel-ui", path = "crates/ui", version = "0.1.29" }
icons = { package = "bezel-icons", path = "crates/icons", version = "0.1.29" }
markdown = { package = "bezel-markdown", path = "crates/markdown", version = "0.1.29" }
blocks = { package = "bezel-blocks", path = "crates/blocks", version = "0.1.29" }
editor = { package = "bezel-editor", path = "crates/editor", version = "0.1.29" }
syntax = { package = "bezel-syntax", path = "crates/syntax", version = "0.1.29" }
syntax-std = { package = "bezel-syntax-std", path = "crates/syntax-std", version = "0.1.29" }
terminal = { package = "bezel-terminal", path = "crates/terminal", version = "0.1.29" }
canvas = { package = "bezel-canvas", path = "crates/canvas", version = "0.1.29" }
# An app rather than a library: `apps/web` renders the same catalog, and
# nothing publishes it.
gallery = { path = "apps/gallery" }

# crates-io
libc = "0.2"
web-time = "1"
alacritty_terminal = "0.26"
tree-sitter = "0.27"
tree-sitter-highlight = "0.27"
tree-sitter-language = "0.1"
tree-sitter-rust = "0.24.2"
tree-sitter-python = "0.25"
tree-sitter-json = "0.24"
tree-sitter-typescript = "0.23"
tree-sitter-go = "0.25"
tree-sitter-bash = "0.25"
tree-sitter-toml-ng = "0.7"
tree-sitter-html = "0.23"
tree-sitter-css = "0.25"
# Lucide. `bezel-icons` names 84 of its 1834 statics; the linker drops the rest.
# The parser gpui paints SVGs with, used by the icon tests to prove a generated
# document renders rather than merely looking like markup.
usvg = "0.46"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"

# Our zed fork, published from crabtalk/zed; the keys are what the source imports.
gpui = { package = "bezel-gpui", version = "0.3.12" }
gpui_platform = { package = "bezel-gpui-platform", version = "0.3.12", features = [
    "font-kit",
] }
gpui_web = { package = "bezel-gpui-web", version = "0.3.12" }

[workspace.lints.rust]
# The objc 0.2 macros expand `cfg(feature = "cargo-clippy")` checks; declare
# the value so rustc's check-cfg pass doesn't warn on every msg_send! site.
unexpected_cfgs = { level = "warn", check-cfg = [
    'cfg(feature, values("cargo-clippy"))',
] }

# Panics keep file and line; the DWARF that carries variable names does not
# survive. Full debuginfo across the dependency graph was the bulk of a 179G
# target directory, and cargo's fingerprint scan over that tree cost more per
# invocation than the compile it was checking.
[profile.dev]
debug = "line-tables-only"

[profile.dev.package."*"]
debug = false
